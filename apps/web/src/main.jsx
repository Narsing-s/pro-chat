import React,{useEffect,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {io} from 'socket.io-client';
import {MessageCircle,Search,Send,Phone,Video,MoreVertical,Check,CheckCheck,LogOut} from 'lucide-react';
import './styles.css';

const API=import.meta.env.VITE_API_URL||(['localhost','127.0.0.1','0.0.0.0'].includes(location.hostname)?'http://localhost:3000':location.origin);
const USER_KEY='pro-chat-user-v3',CHATS_KEY='pro-chat-chats-v3',MSG_KEY='pro-chat-messages-v3',OUTBOX_KEY='pro-chat-outbox-v1';
const cid=(a,b)=>[a,b].sort().join(':');
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}};
if('serviceWorker' in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));
function App(){const me=read(USER_KEY,null);const[chats,setChats]=useState(()=>read(CHATS_KEY,[]));const[messages,setMessages]=useState(()=>read(MSG_KEY,{}));const[search,setSearch]=useState('');const[results,setResults]=useState([]);const[searching,setSearching]=useState(false);const[searchError,setSearchError]=useState('');const[active,setActive]=useState(null);const[text,setText]=useState('');const[online,setOnline]=useState(false);const socketRef=useRef(null);useEffect(()=>localStorage.setItem(CHATS_KEY,JSON.stringify(chats)),[chats]);useEffect(()=>localStorage.setItem(MSG_KEY,JSON.stringify(messages)),[messages]);useEffect(()=>{if(!me?.token)return;const s=io(API,{auth:{token:me.token},transports:['websocket','polling'],reconnection:true});socketRef.current=s;s.on('connect',()=>{setOnline(true);read(OUTBOX_KEY,[]).forEach(m=>s.emit('message',m));localStorage.setItem(OUTBOX_KEY,'[]')});s.on('disconnect',()=>setOnline(false));s.on('message',receive);s.on('message:ack',receive);return()=>s.disconnect()},[me?.token]);function receive(m){if(!m?.chatId||!m?.id)return;setMessages(p=>({...p,[m.chatId]:[...(p[m.chatId]||[]).filter(x=>x.id!==m.id),m].sort((a,b)=>new Date(a.createdAt)-new Date(b.createdAt))}));setChats(p=>p.map(c=>c.id===m.chatId?{...c,lastMessage:m.text,lastAt:m.createdAt}:c))}async function find(q=search){const v=q.trim();setSearchError('');if(!v){setResults([]);return}setSearching(true);try{const r=await fetch(`${API}/api/users?q=${encodeURIComponent(v)}`,{headers:{Authorization:`Bearer ${me.token}`},cache:'no-store'});const d=await r.json().catch(()=>[]);if(!r.ok)throw Error(d.error||`Search failed (${r.status})`);const users=Array.isArray(d)?d.filter(u=>u.id!==me.id):[];setResults(users);if(!users.length)setSearchError('No user found. Try the exact username, email or phone number.')}catch(e){setResults([]);setSearchError(e.message==='Failed to fetch'||e.name==='TypeError'?'Cannot reach the Pro Chat server. Cross-device user search requires the same reachable server.':e.message||'Unable to search users.')}finally{setSearching(false)}}async function openUser(u){if(!u?.id||u.id===me.id)return;const id=cid(me.id,u.id);setActive(id);setSearch('');setResults([]);setSearchError('');setChats(p=>p.some(c=>c.id===id)?p:[{id,userId:u.id,name:u.name||u.username||'Contact',username:u.username||'',email:u.email||'',phoneNumber:u.phoneNumber||'',lastMessage:'',lastAt:u.createdAt||new Date().toISOString()},...p]);socketRef.current?.emit('join',id);try{const r=await fetch(`${API}/api/messages/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${me.token}`},cache:'no-store'});if(r.ok){const d=await r.json();setMessages(p=>({...p,[id]:Array.isArray(d)?d:[]}))}}catch{}}function send(){const v=text.trim(),c=chats.find(x=>x.id===active);if(!v||!c)return;const m={id:crypto.randomUUID(),chatId:active,senderId:me.id,senderName:me.name,senderUsername:me.username,receiverId:c.userId,text:v,createdAt:new Date().toISOString(),status:'sent'};receive(m);setText('');if(socketRef.current?.connected)socketRef.current.emit('message',m);else{const q=read(OUTBOX_KEY,[]);q.push(m);localStorage.setItem(OUTBOX_KEY,JSON.stringify(q))}}function logout(){localStorage.removeItem(USER_KEY);location.replace(location.pathname)}if(!me)return null;const activeChat=chats.find(c=>c.id===active);const list=active?(messages[active]||[]):[];return <div className="app-shell"><aside className="sidebar"><div className="brand"><MessageCircle size={25}/><strong>Pro Chat</strong><span className={online?'online-dot':'offline-dot'}/></div><div className="me-row"><div className="avatar">{(me.username||me.name||'P')[0].toUpperCase()}</div><div className="me-info"><b>@{me.username||me.name}</b><small>{me.email||me.phoneNumber||''}</small></div><button onClick={logout}><LogOut size={18}/></button></div><div className="search-box"><Search size={18}/><input value={search} onChange={e=>{setSearch(e.target.value);if(!e.target.value.trim()){setResults([]);setSearchError('')}}} onKeyDown={e=>e.key==='Enter'&&find()} placeholder="Search username, email or phone"/><button onClick={()=>find()} disabled={searching}>{searching?'…':'Search'}</button></div>{(results.length||searchError)&&<div className="search-results">{results.map(u=><button className="user-result" key={u.id} onClick={()=>openUser(u)}><div className="avatar">{(u.username||u.name||'P')[0].toUpperCase()}</div><div><b>@{u.username||u.name}</b><span>{u.name}{u.email?' • '+u.email:''}</span></div><MessageCircle size={18}/></button>)}{searchError&&<div className="search-error">{searchError}</div>}</div>}<div className="chat-list">{chats.map(c=><button key={c.id} className={'chat-row '+(active===c.id?'active':'')} onClick={()=>openUser(c)}><div className="avatar">{(c.username||c.name||'P')[0].toUpperCase()}</div><div className="chat-meta"><b>@{c.username||c.name}</b><span>{c.lastMessage||'Start a conversation'}</span></div></button>)}</div></aside><main className="chat-panel">{activeChat?<><header className="chat-header"><div className="avatar">{(activeChat.username||activeChat.name||'P')[0].toUpperCase()}</div><div><b>@{activeChat.username||activeChat.name}</b><small>{activeChat.name}{activeChat.email?' • '+activeChat.email:''}</small></div><div className="chat-actions"><Phone size={19}/><Video size={20}/><MoreVertical size={20}/></div></header><section className="messages">{list.map(m=><div key={m.id} className={'bubble '+(m.senderId===me.id?'mine':'theirs')}><span>{m.text}</span><small>{new Date(m.createdAt).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})} {m.senderId===me.id&&(m.status==='sent'?<Check size={13}/>:<CheckCheck size={13}/>)}</small></div>)}</section><footer className="composer"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&!e.shiftKey&&(e.preventDefault(),send())} placeholder="Type a message"/><button onClick={send} disabled={!text.trim()}><Send size={20}/></button></footer></>:<div className="empty-state"><MessageCircle size={56}/><h2>Pro Chat</h2><p>Search a username, email or phone number to start a private conversation.</p></div>}</main></div>}
createRoot(document.getElementById('root')).render(<App/>);

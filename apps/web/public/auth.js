(() => {
  const USER_KEY='pro-chat-user-v3';
  const LOCAL_ACCOUNT_KEY='pro-chat-local-account-v1';
  const isLocal = ['localhost','127.0.0.1','0.0.0.0'].includes(window.location.hostname);
  const API = window.__PRO_CHAT_API__ || (isLocal ? 'http://localhost:3000' : '');
  const style=document.createElement('style');style.textContent=`.pro-auth-overlay{position:fixed;inset:0;z-index:99999;display:grid;place-items:center;background:radial-gradient(circle at 20% 10%,#173b70 0,#07111f 38%,#050811 100%);padding:20px;font-family:system-ui,-apple-system,sans-serif}.pro-auth-card{width:min(420px,100%);background:#fff;border-radius:24px;padding:30px;box-shadow:0 24px 80px #0008;color:#101828}.pro-auth-logo{width:54px;height:54px;border-radius:16px;display:grid;place-items:center;background:#111827;color:#fff;font-size:26px;font-weight:800;margin-bottom:16px}.pro-auth-card h1{margin:0 0 6px;font-size:28px}.pro-auth-card p{margin:0 0 22px;color:#667085}.pro-auth-card label{display:block;font-size:13px;font-weight:700;margin:12px 0 6px}.pro-auth-card input{width:100%;box-sizing:border-box;border:1px solid #d0d5dd;border-radius:12px;padding:12px 13px;font-size:15px;outline:none}.pro-auth-card input:focus{border-color:#111827;box-shadow:0 0 0 3px #11182712}.pro-auth-primary{width:100%;border:0;border-radius:12px;padding:13px;margin-top:18px;background:#111827;color:#fff;font-size:15px;font-weight:700;cursor:pointer}.pro-auth-primary:disabled{opacity:.6;cursor:wait}.pro-auth-switch,.pro-auth-forgot{border:0;background:none;color:#344054;cursor:pointer;font-size:14px}.pro-auth-switch{margin-top:16px;width:100%;text-decoration:underline}.pro-auth-forgot{display:block;margin:12px auto 0;text-decoration:underline}.pro-auth-error{min-height:20px;margin-top:12px;color:#b42318;font-size:13px}.pro-auth-success{color:#027a48}.pro-auth-note{font-size:12px;color:#667085;margin-top:14px;text-align:center;line-height:1.5}`;document.head.appendChild(style);
  const originalFetch=window.fetch.bind(window);
  const request=async(path,body)=>{if(!API)throw new Error('OFFLINE_ONLY');let r;try{r=await originalFetch(`${API}${path}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});}catch{throw new Error('OFFLINE_ONLY')}const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Request failed (${r.status})`);return data};
  const hex=buf=>Array.from(new Uint8Array(buf),b=>b.toString(16).padStart(2,'0')).join('');
  async function derive(password,salt){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(password),'PBKDF2',false,['deriveBits']);const bits=await crypto.subtle.deriveBits({name:'PBKDF2',salt:new TextEncoder().encode(salt),iterations:150000,hash:'SHA-256'},key,256);return hex(bits)}
  const token=()=>crypto.randomUUID()+'.'+crypto.randomUUID();
  const norm=v=>String(v||'').trim().toLowerCase();
  const getLocal=()=>{try{return JSON.parse(localStorage.getItem(LOCAL_ACCOUNT_KEY)||'null')}catch{return null}};
  const saveLocal=async({email,phoneNumber,username,password,name})=>{const salt=crypto.randomUUID();const account={id:'local-'+crypto.randomUUID(),name,displayName:name,email,phoneNumber,username,passwordHash:await derive(password,salt),salt,createdAt:new Date().toISOString(),offline:true};localStorage.setItem(LOCAL_ACCOUNT_KEY,JSON.stringify(account));return {...account,token:token(),offline:true}};
  const localLogin=async(identifier,password)=>{const a=getLocal();if(!a)throw new Error('No offline account exists on this device. Create an account first.');const q=norm(identifier);if(![a.email,a.phoneNumber,a.username].map(norm).includes(q))throw new Error('Username, email, or phone number is not recognized on this device.');if(await derive(password,a.salt)!==a.passwordHash)throw new Error('Incorrect password.');return {...a,token:token(),offline:true}};
  const saveUser=user=>{if(!user?.id||!user?.token)throw new Error('Invalid account session.');localStorage.setItem(USER_KEY,JSON.stringify(user));window.location.replace(window.location.pathname)};
  function mount(){if(document.querySelector('.pro-auth-overlay'))return;const params=new URLSearchParams(location.search),resetToken=params.get('reset');const existing=(()=>{try{return JSON.parse(localStorage.getItem(USER_KEY))}catch{return null}})();if(existing?.token&&!resetToken)return;const overlay=document.createElement('div');overlay.className='pro-auth-overlay';overlay.innerHTML=`<div class="pro-auth-card"><div class="pro-auth-logo">P</div><h1 id="pro-auth-title">Welcome to Pro Chat</h1><p id="pro-auth-sub">Private messaging, designed to work offline.</p><div id="pro-auth-fields"></div><div class="pro-auth-error" id="pro-auth-msg"></div><button type="button" class="pro-auth-primary" id="pro-auth-submit">Create Account</button><button type="button" class="pro-auth-switch" id="pro-auth-switch">Already have an account? Login</button><button type="button" class="pro-auth-forgot" id="pro-auth-forgot">Forgot Password?</button><div class="pro-auth-note">Offline accounts are stored on this device. Your password is protected with PBKDF2 and is never stored as plain text.</div></div>`;document.body.appendChild(overlay);const fields=overlay.querySelector('#pro-auth-fields'),submit=overlay.querySelector('#pro-auth-submit'),sw=overlay.querySelector('#pro-auth-switch'),forgot=overlay.querySelector('#pro-auth-forgot'),msg=overlay.querySelector('#pro-auth-msg'),sub=overlay.querySelector('#pro-auth-sub'),title=overlay.querySelector('#pro-auth-title');let register=true,resetMode=Boolean(resetToken);const input=(id,label,type,placeholder,autocomplete='on')=>`<label for="${id}">${label}</label><input id="${id}" type="${type}" placeholder="${placeholder}" autocomplete="${autocomplete}">`;
    function render(){msg.textContent='';msg.className='pro-auth-error';if(resetMode){title.textContent='Reset your password';sub.textContent='Password reset is available when a server is configured.';fields.innerHTML=input('password','New password','password','At least 8 characters','new-password')+input('confirm','Confirm password','password','Re-enter your new password','new-password');submit.textContent='Reset Password';sw.textContent='Back to Login';sw.style.display='block';forgot.style.display='none'}else if(register){title.textContent='Create your Pro Chat account';sub.textContent='Your account works without internet on this device.';fields.innerHTML=input('email','Email','email','you@example.com')+input('phone','Phone number','tel','+91 98765 43210')+input('username','Username','text','Choose a username','username')+input('password','Password','password','At least 8 characters','new-password')+input('confirm','Confirm password','password','Re-enter your password','new-password');submit.textContent='Create Account';sw.textContent='Already have an account? Login';sw.style.display='block';forgot.style.display='none'}else{title.textContent='Welcome back';sub.textContent='Login offline with your username, email, or phone.';fields.innerHTML=input('identifier','Username, Email or Phone','text','Username / email / phone','username')+input('password','Password','password','Your password','current-password');submit.textContent='Login';sw.textContent='New to Pro Chat? Create Account';sw.style.display='block';forgot.style.display='block'}fields.querySelector('input')?.focus()}
    render();sw.onclick=()=>{if(resetMode){resetMode=false;register=false;history.replaceState({},'',location.pathname);render();return}register=!register;render()};forgot.onclick=()=>{msg.textContent='Offline password recovery is disabled by design because there is no trusted recovery service. Use your device backup/recovery copy or create a new local account.';msg.className='pro-auth-error'};
    submit.onclick=async()=>{msg.textContent='';msg.className='pro-auth-error';submit.disabled=true;try{let user;if(resetMode)throw Error('Password reset requires a configured server.');if(register){const email=fields.querySelector('#email').value.trim(),phoneNumber=fields.querySelector('#phone').value.trim(),username=fields.querySelector('#username').value.trim(),password=fields.querySelector('#password').value,confirm=fields.querySelector('#confirm').value;if(!email||!phoneNumber||!username||!password||!confirm)throw Error('Please fill in all fields.');if(password!==confirm)throw Error('Passwords do not match.');if(password.length<8)throw Error('Password must be at least 8 characters.');try{user=await request('/api/auth/register',{email,phoneNumber,username,password})}catch{if(getLocal())throw Error('An offline account already exists on this device.');user=await saveLocal({email,phoneNumber,username,password,name:username})}}else{const identifier=fields.querySelector('#identifier').value.trim(),password=fields.querySelector('#password').value;if(!identifier||!password)throw Error('Enter your login details.');try{user=await request('/api/auth/login',{identifier,password})}catch{user=await localLogin(identifier,password)}}saveUser(user)}catch(e){msg.textContent=e.message||'Unable to complete request.'}finally{submit.disabled=false}};
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();
})();
